// Secrets.swift.sample — Commit this file. Copy to Tempo/Utilities/Secrets.swift and fill in real values.
// The real Secrets.swift is gitignored and must never be committed.

enum Secrets {
    static let geminiAPIKey = "YOUR_GEMINI_API_KEY_HERE"
}
